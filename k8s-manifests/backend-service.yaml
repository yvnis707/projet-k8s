apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000        # port interne Kubernetes
      targetPort: 5000  # port dans le pod
      nodePort: 30001   # port sur la VM pour accéder depuis ton PC
